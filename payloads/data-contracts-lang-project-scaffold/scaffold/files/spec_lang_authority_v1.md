# Spec-Lang Authority Contract (v1)

This repository owns spec-lang language contracts, libraries, and conformance cases.
Schema authority remains in `data-contracts` and is consumed through `schema_ref`.
